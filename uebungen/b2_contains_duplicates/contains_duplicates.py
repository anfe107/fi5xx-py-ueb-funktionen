def contains_duplicates(elements: list[int]) -> bool:
    """Check whether the list contains any duplicate elements.

    elements: list of integers (may be empty).
    Return: True if at least one element appears more than once; False
        otherwise. An empty list returns False.
    """
    # TODO
